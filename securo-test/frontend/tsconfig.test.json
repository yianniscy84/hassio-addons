{
  /* Test files need the app's world, not the Node one: the DOM lib, JSX, the
     `@/` alias, and the jest-dom matchers that `src/test/setup.ts` registers.
     They used to be type-checked by tsconfig.node.json, which has none of
     those — workable while every test covered a pure function with relative
     imports, but not once they render components. */
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.test.tsbuildinfo",
    "target": "ES2022",
    "useDefineForClassFields": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "types": ["vite/client", "node"],
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",

    /* TS 7 removed baseUrl; paths resolve relative to this file. */
    "paths": {
      "@/*": ["./src/*"]
    },

    /* Linting */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedSideEffectImports": true
  },
  /* `vite-env.d.ts` declares the build-time `__APP_VERSION__` global that
     vitest.config.ts defines. A test rendering the app shell reaches
     lib/build-info through its imports, so this project needs the
     declaration too. */
  "include": [
    "src/**/*.test.ts",
    "src/**/*.test.tsx",
    "src/test/**/*",
    "src/vite-env.d.ts"
  ]
}
